{namespace Header}

/**
 * Prints the Hello Soy portlet header.
 */
{template .render}


	<blockquote class="blockquote-primary">
		<p>{msg desc=""}Es necesario iniciar sesion{/msg}</p>
	</blockquote>
{/template}